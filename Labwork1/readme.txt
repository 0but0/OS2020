The syntax is very new to us so it was quite hard to figure out the solutions. Therefore we follow your solution:
-We can run 1 command (ex: ls, cd, ps -ef) and 2 commands "ps -ef | grep firefox" 
-We can't run more than 2 commands